# 目标金额与有流水持仓不可变

Portfolio.targetTotalAmount 若允许修改，历史 DailySnapshot 的 completion 会随分母变化而回退，增删有流水的 Holding 会级联删 Trade 导致 ΣBUY 与 totalPnl 重算；决定目标金额创建后不可改、有已确认 Trade 的 Holding 不可删，保证快照历史可解释且 completion 作为阶段开关只增不减。
